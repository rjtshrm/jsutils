# jsutils

## Web App

A simple browser-based PDF tool set is provided under `webapp/`. Open `webapp/index.html` in a modern browser to use features like:

- Convert images to a PDF
- Convert PDFs to images
- Merge multiple PDFs
- Split a PDF by page range

All processing happens in the browser using JavaScript libraries.
